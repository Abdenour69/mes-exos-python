def len(obj):
    total=0
    for i in obj:
        total= total + 1
        return total
    


def est_palindrome(mot):
    longueur = len(mot)
    for i in range(0, 4):
       if mot[i] = mot[-i]
          return false
return True
print(est_palindrome("radar")) # Affiche true